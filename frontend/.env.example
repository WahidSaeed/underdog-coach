# Base URL of the deployed API (backend/main.py, see template.yaml
# Outputs.ApiUrl), or http://localhost:8000 for `uvicorn main:app --reload`.
# This is a client-side (NEXT_PUBLIC_*) var because the frontend is a
# static export with no server-side proxying.
NEXT_PUBLIC_API_URL=http://localhost:8000
